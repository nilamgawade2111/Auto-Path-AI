apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: car-ingressroute
  namespace: default
  labels:
    app: car
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`car.localhost`)
      kind: Rule
      services:
        - name: car
          namespace: default
          port: 8000
          scheme: http
          strategy: RoundRobin
          healthCheck:
            path: /
            interval: 10s
            timeout: 3s
            scheme: http
      middlewares:
        - name: default-retry
        - name: default-circuitbreaker
        - name: default-compress
        - name: default-buffering
        - name: dev-headers
    - match: Host(`car.framework.autopath.ai`)
      kind: Rule
      services:
        - name: car
          namespace: default
          port: 8000
          scheme: http
          strategy: RoundRobin
          healthCheck:
            path: /
            interval: 10s
            timeout: 3s
            scheme: http
      middlewares:
        - name: default-retry
        - name: default-circuitbreaker
        - name: default-compress
        - name: default-buffering
        - name: default-headers

